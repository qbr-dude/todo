import React from 'react'

type Props = {}

const Input = (props: Props) => {

    const handleChange = () => {

    }

    return (
        <input
            className={''}
            type="text"
            onChange={handleChange}
        />
    )
}

export default Input